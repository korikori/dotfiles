(if
  (matches (application_name) "python2")
  (begin
    (stick)
    (above)
    (skip_pager)
    (skip_tasklist)
    (wintype "dock")
    (pin)
    
  )
)

(if
  (matches (window_name) "pyICDsemafor-admin")
  (begin
    (geometry "100x40+1790+1015")
  )
)
(if
  (matches (window_name) "pyICDsemafor/0.1")
  (begin
    (geometry "100x40+1700+955")
  )
)
